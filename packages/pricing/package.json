{
  "name": "@petappro/pricing",
  "version": "0.1.0",
  "description": "PetAppro pricing engine — pure function, server-side, integer minor units",
  "private": true,
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "typescript": "^5.5.0",
    "vitest": "^1.6.0"
  }
}
