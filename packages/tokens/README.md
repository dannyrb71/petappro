# @petappro/tokens (build target — not yet scaffolded)

**Generated output. Do not hand-edit.** These are the platform-ready token outputs the
app imports (TS/JS constants, and whatever native/web formats we target), built from the
authored source at `design-system/tokens/*.tokens.json`.

- **Source of truth:** `design-system/tokens/` (DTCG). Edit tokens there, then rebuild.
- **Build:** TBD (e.g. Style Dictionary) — wired by Claude Code when the Expo monorepo is set up.
- **Governance:** the source is linted by `design-system/tools/lint-tokens.mjs` before it builds.

Until the monorepo/workspaces are initialized, this is a placeholder marking where generated
tokens will live so nothing lands in the wrong place.
